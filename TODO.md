# TODO - Neaten web structure

- [x] Fix document structure + markup issues in `AsepStore.com/index.html` (remove duplicate head/body, fix broken category link, fix invalid input attribute, clean up obvious wrapper nesting).
- [ ] Apply the same neatening pattern to other pages in `AsepStore.com/` **once their contents can be read reliably** (products/about/services/contact). If read fails, fall back to minimal structural fixes without overwriting unknown content.
- [x] Run a quick validation: check for well-formed HTML and confirm there are no broken tags.



